<h1>Groups</h1>
<% @groups.each do |group| %>
  <%= group.id %><br>
  <%= group.owner_id %><br>
  <%= group.name %><br>
  <%= group.introduction %><br>
  <%= link_to "Edit", edit_group_path(group.id) %><br>
<% end %>